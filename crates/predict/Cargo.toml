[package]
name = "predict"
version = "0.1.0"
authors = ["massivelivefun <matthewlancefuller@gmail.com>"]
edition = "2024"

[dependencies]
candle-core = "0.8.0"
candle-nn = "0.8.0"
model_lib = { path = "../model_lib" }
